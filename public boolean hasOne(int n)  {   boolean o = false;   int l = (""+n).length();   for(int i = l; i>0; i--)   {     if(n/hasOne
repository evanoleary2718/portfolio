public boolean hasOne(int n) 
{
  boolean o = false;
  int l = (""+n).length();
  for(int i = l; i>0; i--)
  {
    if(n/(int)(Math.pow(10,i-1)) == 1)
    {
      o = true;
    }
    n = (int)(n%Math.pow(10,i-1));
  }
  return o;
}
